({
    doInit : function(component, event, helper) {
        var myNames = component.get("v.body");  
        alert("body value = " + myNames );
        //component.set("v.body","I like Pizza");
    }
})
